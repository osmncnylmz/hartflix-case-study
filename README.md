# Hartflix (Flutter Case Study)
## Setup
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter run
## Features
Auth (secure token), Explore (infinite scroll, 5/page, pull-to-refresh, optimistic fav),
Profile, BottomNav, Bloc, Clean Architecture, GoRouter, Theme, Splash, App Icon.
